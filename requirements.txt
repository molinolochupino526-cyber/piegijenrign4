# Основные зависимости генератора обучающих видео
moviepy>=2.0.0          # монтаж видео (поддерживается и ветка 1.0.3, см. README)
imageio-ffmpeg>=0.4.9   # поставляет бинарник ffmpeg для moviepy
Pillow>=10.0.0          # рендер текста (используется внутри moviepy 2.x)
numpy>=1.24.0
requests>=2.31.0        # HTTP-запросы к TTS API
python-dotenv>=1.0.0    # чтение ключей из .env
mutagen>=1.47.0         # быстрое определение длительности MP3
